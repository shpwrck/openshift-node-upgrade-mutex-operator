---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mutexes.openshift-optional.skrzypek.io
spec:
  group: openshift-optional.skrzypek.io
  names:
    kind: Mutex
    listKind: MutexList
    plural: mutexes
    singular: mutex
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Mutex is the Schema for the mutexes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of Plan resource
            properties:
              aap:
                description: Configuration for Ansible Automation Platform
                properties:
                  controller_host:
                    description: "Server address of AAP (without protocol)"
                    type: string
                    pattern: '^.+$'
                  controller_oauthtoken:
                    description: "OAuthToken generated with {{controller_host}}/api/v2/tokens/"
                    type: string
                    pattern: '^.+$'
                  job_template:
                    description: 'Name of the job template being launched'
                    type: string
                    pattern: '^.+$'
                required:
                - controller_host
                - controller_oauthtoken
                - job_template
                type: object
            type: object
          status:
            description: Status defines the observed state of Mutex
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
